[Unit]
Description=SGG auto-update agent (cada 15 min)

[Timer]
OnCalendar=*:00/15
# Recupera la corrida perdida si la VM estuvo apagada (§4.2 del plan).
Persistent=true
RandomizedDelaySec=60

[Install]
WantedBy=timers.target
