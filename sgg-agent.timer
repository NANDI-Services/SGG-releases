[Unit]
Description=SGG auto-update agent (cada 5 min)

[Timer]
OnCalendar=*:00/5
# Recupera la corrida perdida si la VM estuvo apagada (§4.2 del plan).
Persistent=true
RandomizedDelaySec=60

[Install]
WantedBy=timers.target
